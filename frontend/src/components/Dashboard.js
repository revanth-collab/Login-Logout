import React from "react";
import Header from "./Header";

const Dashboard = () => {
    return (
        <div style={{ width: "100vw" }}>
            <Header />
            <h1 style={{ color: 'black', padding: '10px 50px' }}>Welcome to the Dashboard</h1>
        </div>
    );
};

export default Dashboard;
